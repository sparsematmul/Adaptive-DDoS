







function static_defense(pkt) {


	let traffic_stats = diagnose_traffic(pkt)
	static_mitigation(ingress_locations)

}



function dynamic_defense(pkt):
	let traffic_stats = diagnose_traffic(pkt)
	dynamic_mitigation(ingress_locations, totalCapacity)
